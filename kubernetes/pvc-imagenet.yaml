apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: imagenet-data-mount
spec:
  storageClassName: seaweedfs-storage-nvme
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
